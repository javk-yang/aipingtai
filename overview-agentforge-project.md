# 项目二：AgentForge —— 企业级 AI Agent 智能体平台

**技术栈：** Spring Boot 3 · Vue 3 · TypeScript · LangGraph · Python · MySQL · Redis · SSE · MCP · OpenAI-Compatible API · DeepSeek · Qwen · Ollama

**多模型统一接入与运行时选择：** 构建统一的大模型配置与调用链路，支持 deterministic、DeepSeek、OpenAI-compatible、Qwen、Ollama 等多类模型提供商。系统按照“请求级模型配置 → 会话绑定 Agent 模型 → 平台默认模型”的优先级完成运行时选择，并对 401、402、403、404、429、超时、代理连接失败及上游 5xx 等异常进行分类处理，支持安全降级与可解释错误反馈，避免模型选择被错误切回内置确定性模型。

**Agent 智能体编排平台：** 基于 LangGraph 构建可配置的 Agent 运行时，支持智能体的创建、编辑、发布、启停、删除及会话级复用。每个 Agent 可独立配置系统提示词、绑定模型、工具、技能与知识库资源，运行时通过白名单机制限制实际可调用能力，确保 Agent 配置与执行权限一致，形成从“智能体配置”到“对话执行”的完整闭环。

**MCP 工具与 Skill 技能系统：** 实现工具注册中心、工具调用网关、技能注册中心与技能运行引擎，支持工具的新增、编辑、启停和删除，以及技能的手动创建和 `.skillzip` 技能包导入。技能包导入过程包含文件大小限制、解压总量限制、目录穿越防护、绝对路径拦截、危险文件扩展名校验、唯一 `SKILL.md` 检查、YAML front matter 校验、租户目录隔离、原子移动、数据库事务和失败回滚，满足企业级技能扩展与安全治理要求。

**流式对话与安全过程可视化：** 通过 SSE/NDJSON 打通 Vue 前端、Java 后端与 Python Agent 引擎，支持文本增量、工具调用、技能执行、知识库检索、错误和完成状态等事件的实时推送。前端以“分析摘要、行动、工具、技能、知识库、生成、完成”等阶段展示可审计过程，但不暴露隐藏逐字思维链、内部规划 JSON 或原始协议内容；同时对历史消息和模型输出进行清洗，避免 `tool_calls` JSON、工具参数及内部错误堆栈泄漏给用户。

**RAG 知识库与上下文治理：** 构建知识库文档导入、内容分块、检索增强和来源溯源能力，支持 Agent 按绑定的知识库文档范围执行检索。系统对历史会话进行上下文清洗，提取最新用户问题并过滤错误保存的工具协议，降低多轮对话中的历史污染、重复回复和当前问题被忽略等风险，提升复杂问答场景下的稳定性与可控性。

**可观测性与审计工程：** 集成用量统计、Token 配额预检、API 使用记账、操作审计和工具/技能调用审计，记录模型选择、Agent 运行、工具执行、技能执行及异常降级等关键事件。平台提供可观测页面和审计查询接口，用于分析模型调用情况、排查链路故障、定位性能问题，并为后续成本控制和生产运营提供数据基础。

**企业级安全与可靠性：** 完善认证授权、Token 刷新、租户资源隔离、模型 API Key 脱敏、代理绕过、本地服务健康检查和错误分类机制。针对真实模型连接失败、旧进程占用端口、旧 JAR 未加载、SSE 事件拆分、历史上下文污染等问题建立自动化回归测试，覆盖普通问答、多轮问答、模型失败降级、工具调用、技能调用、会话删除和技能包安全导入等核心场景。

**工程化交付：** 采用 Spring Boot 3 + Vue 3 + Python Agent Engine 的前后端分离架构，配备 MySQL、Redis、统一启动脚本、Vite 生产构建、Java Maven 构建和端到端冒烟测试。支持本地一键启动 Java 后端、Python 引擎、Vue 前端及基础依赖，并通过 30 分钟稳定性巡检验证服务健康、聊天链路、SSE 完成事件和核心接口的持续可用性。
