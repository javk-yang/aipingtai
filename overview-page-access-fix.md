# AgentForge 页面无法访问修复记录

## 根因

原启动脚本把 Python 引擎和 Vite 前端绑定到 `127.0.0.1`，只能由当前机器本机访问；同时后台服务在命令返回后可能被运行环境回收，导致页面短时间后返回 502。

## 已处理

- `scripts/start-all.sh` 新增 `AGENTFORGE_BIND_HOST` 配置，默认绑定 `0.0.0.0`。
- 自动读取本机局域网 IP（当前为 `10.0.4.17`）。
- Python Uvicorn 和前端 Vite 均支持局域网访问。
- 保留本机回环地址 `127.0.0.1:5173`。
- 启动脚本增加健康检查和就绪等待。

## 当前地址

- 本机访问：`http://127.0.0.1:5173`
- 局域网访问：`http://10.0.4.17:5173`

## 验证

- `http://127.0.0.1:5173/` → HTTP 200
- `http://10.0.4.17:5173/` → HTTP 200

如果其他设备访问，必须与当前电脑处于同一局域网，并允许 macOS 防火墙放行 5173 端口。
